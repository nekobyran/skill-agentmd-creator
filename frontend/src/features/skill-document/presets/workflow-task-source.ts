// Mechanical UTF-8 snapshot of project-workflow-task/SKILL.md.
// SHA256: 60ECA37CC14C036A68D2021F0201C4C786C9FB6DCB711E83EF81797998A0D123
export const WORKFLOW_TASK_SOURCE_SHA256 = "60ECA37CC14C036A68D2021F0201C4C786C9FB6DCB711E83EF81797998A0D123";
export const WORKFLOW_TASK_SOURCE_BYTE_COUNT = 10586;
export const WORKFLOW_TASK_SOURCE_LINE_COUNT = 159;
export const WORKFLOW_TASK_SOURCE_H2_COUNT = 11;
export const WORKFLOW_TASK_SOURCE_FENCED_CODE_BLOCK_COUNT = 6;
export const WORKFLOW_TASK_SOURCE_H2_HEADINGS = ["使用时机","脚本入口","基础输入","常用动作","Agent 协作规则","多 Task 与陈旧降噪","目录 Scope 与冲突判断","冲突自动转交","Final Audit 协调","Complete 要求","汇报要求"];
export const WORKFLOW_TASK_SOURCE = "---\nname: project-workflow-task\ndescription: \"项目 workflow-task 脚本管理 skill。用于普通修改及以上任务的 task/log 管理、冲突检查、计划追加、协作 agent 加入/消息/共享上下文、Codex CLI agent 命令生成、Flutter UI 代码语义完成度审查条目、子任务记录、Final Audit 子任务、audit-check 门禁、self-test、陈旧 task 清理/降噪、基础教程和完成归档；父任务 complete 前必须通过 project-quality-gate。\"\n---\n\n# Project Workflow Task\n\n## 使用时机\n\n当 `project-task-intake` 判定 `workflow_required: yes` 时使用本 skill。通常包括普通修改、大型/高风险修改、高难度/复杂任务，以及全局规则、workflow、skill 或质量门禁维护。\n\n本 skill 只管理中央 workflow 状态。项目构建/运行脚本交给 `project-command-ops`；验证和 Final Audit 交给 `project-quality-gate`。\n\n## 脚本入口\n\n- 固定使用 `D:\\vibecoding\\command\\workflow-task.ps1`。\n- task/log 事实来源固定为 `D:\\vibecoding\\task` 和 `D:\\vibecoding\\log`。\n- 不把项目 `command/` 下的脚本作为 workflow 入口、备用入口或恢复入口。\n- 中央脚本不存在或执行失败时，先修复该脚本并继续，不绕过 task/log 管理。\n\n查看基础教程：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action help\n```\n\n## 基础输入\n\n`start` 和 `subtask` 的必填输入：\n\n- `-Request`：用户要求原文。多条可用换行、分号或 `|` 分隔。\n- `-Files`：改动文件或目录。可写目录，例如 `lib`、`project/app/android/app/src/main/res`；多个值用分号分隔。\n- `-Plan`：计划或验收项。多条可用换行、分号或 `|` 分隔。\n\nFlutter UI 普通修改及以上任务的 `-Plan` 必须包含 `代码语义完成度审查` 条目，覆盖 Material/project 原语选择、页面/路由语义、主从层级、卡片/列表语义、介绍文本必要性和动效落地；缺少该条目时先追加计划再实施或完成。\n\n可选输入：\n\n- `-Projects`：兼容旧任务字段，可传但不必填。\n- `-Feature`：功能名；未传时默认取第一条请求。\n- `-Platforms`：仅当端范围明确时传，例如 `Windows;Android`，不要传空平台。\n\n推荐非交互式调用：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action start -Request \"...\" -Files \"lib; pubspec.yaml\" -Plan \"...\" -Feature \"...\" -Platforms \"Windows;Android\" -NoPrompt\n```\n\n交互式调用只在需要人工逐项输入时使用：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action start -Prompt\n```\n\n## 常用动作\n\n- `start`：正式修改前创建父 task，写入请求、计划、scope，并检查候选冲突。\n- `summary`：不传 `-TaskId` 时列出 active task 概览；传 `-TaskId` 时查看单个 task 详情。\n- `check`：检查指定 task 的候选冲突。\n- `conflict`：把当前/来源 task 的请求转交到确定冲突的目标 task，追加 request/plan/scope，并写入 `handoffs`。\n- `append`：只用于追加新的用户要求和计划，必须传 `-Request` 与 `-Plan`。\n- `item-done`：完成请求项或验收项时写入证据，优先用 `-Note` 记录命令、结果和路径。\n- `subtask`：为真实并行工作或 Final Audit 创建子 task，自动写入父 task `parentContext`。\n- `audit-check`：Final Audit 后、父 task complete 前执行机器门禁。\n- `cleanup`：自动清理安全陈旧任务。默认 dry-run；只有 `-Apply` 才归档 `all-items-done` 或 `joined` 的陈旧 task，不自动归档未完成 `active` task。\n- `join`：所有 task 默认可发现可加入。agent 可直接加入现有 task；也可传 `-FromTaskId` 把自己已有父/子 task 并入目标 task 后继续，来源 task 标记为 `joined`，目标 task 记录 handoff、participant 和消息。\n- `message`：在 task 内给全部 agent 或指定 `-ToAgent` 留消息，用于同步任务进展、阻塞点和审查意见。\n- `context`：写入共享上下文库，推荐存索引、长期要求、接口约定、决策、证据路径、风险、运行命令和边界结论。\n- `spawn`：生成并记录高自由度 Codex CLI agent 命令，用于绕开客户端子 agent 限流并提高执行自由度。默认只输出命令和写入 task，显式 `-Apply` 才启动；启动前会真实探测模型，不可用则写入 `blocked-model-unavailable` 并停止。新 agent 必须先 `join` 再工作。\n- `resume-agent`：为已结束或可继续的 Codex session 生成重新唤起命令，并记录到 task 消息。\n- `model-test`：真实调用 Codex CLI 探测 `spark/gpt5.3spark` 与 `professional/gpt5.5` 当前账号是否可用；可配合 `-TaskId` 把结果写入共享上下文。\n- `self-test`：修改 `workflow-task.ps1` 后执行脚本级冒烟验证；可配合临时 `-WorkflowRoot` 避免污染中央 task/log。\n- `complete`：只能在 `project-quality-gate` 通过后调用，归档最终修改、验证证据、跳过项和剩余风险。\n\n## Agent 协作规则\n\n- 主 agent 默认承担总审查：合并前审查子 agent 修改、验证证据、冲突范围和剩余风险。\n- 子 agent 必须承担真实等量实现、验证或审查任务，不得只做只读搜索；只读搜索应作为实现/验证的前置证据，不算独立完成分工。\n- 默认推荐通过本脚本 `spawn` 生成 Codex CLI agent 命令，避免绕过 task/log、消息和共享上下文；脚本生成的命令默认使用 CLI 高自由度执行参数。\n- 子 agent、嵌套子 agent、已存在 task 都必须先 `join` 到目标 task；需要并入时使用 `join -TaskId target -FromTaskId source`。\n- 子 agent 可以继续 `spawn` 下级 agent，但下级也必须先 `join`，并用 `message/context` 写明分工、范围和证据。\n- 子 agent 模型入口只保留两档：`-AgentType spark` 映射 `gpt-5.3-codex-spark`，用于快速任务子 agent；`-AgentType professional` 映射 `gpt-5.5`，用于专业子 agent。\n- `gpt-5.3-codex-spark` 是快速模型，适合小范围实现、验证和并行补证据；`gpt-5.5` 是高智商模型，适合架构拆分、复杂合并和总审查。\n- 子 agent 命令必须统一设置 `model_reasoning_effort=\"high\"`。\n- 若 `model-test` 证明当前账号不支持某档模型，不得伪造可用；`spawn -Apply` 必须 fail-fast。\n- 已结束 agent 可用 `resume-agent -SessionId ...` 生成唤起命令；恢复后仍需先读取 `summary`、`messages` 和 `sharedContext`。\n\n## 多 Task 与陈旧降噪\n\n当 active task 很多时：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action summary\n```\n\n常用筛选参数：\n\n- `-Limit 20`：调整 summary/check 显示数量。\n- `-StaleMinutes 120`：调整陈旧阈值。\n- `-IncludeStale`：显示陈旧候选。\n- `-StaleOnly`：只看陈旧候选。\n\n默认冲突检查只详细展示新近 task 和高重合陈旧 task；陈旧低重合 task 只做摘要提醒。不要因为降噪而忽略同文件、同目录、同功能或同平台的真实冲突。\n\n## 目录 Scope 与冲突判断\n\n- `-Files` 可以写文件或目录；目录与其子文件互相视为重合。\n- 路径按 `/` 风格规范化，尾斜杠不影响判断。\n- 示例：`lib` 与 `lib/main.dart` 算同范围；`lib` 与 `library` 不算同范围。\n- 不要传空文件列表或虚构路径；可以传计划中将要创建的真实目录。\n\n## 冲突自动转交\n\n确定文件/目录冲突时，必须自动转交，不允许继续创建或执行平行修改 task。\n\n强制规则：\n\n- `start` 会在创建新 task 前检查新近 active task；若存在确定文件/目录重叠，会把本次 `Request` / `Plan` / `Files` / `Feature` 自动追加到最匹配目标 task，并写入 `handoffs`，然后停止创建重复 task。\n- `check` 显示 `同文件` 或 `FileOverlap > 0` 且目标不是陈旧低风险任务时，当前 agent 必须停止直接编辑，使用 `conflict` 转交到目标 task，或明确完成/归档目标 task 后再继续。\n- `conflict` 用法：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action conflict -TaskId target-task -FromTaskId source-task -Request '...' -Plan '...' -Files '...'\n```\n\n- `lib`、`test`、`src` 等短目录 scope 会与更长路径中的同名目录重叠，例如 `lib` 与 `project/app/lib/main.dart` 视为确定文件冲突。\n- 只有无文件/目录重叠的同平台、同工程或同功能候选，才允许由 agent 判断为可并行；若判断为可并行，需在 task note 或最终风险中说明边界。\n\n## Final Audit 协调\n\n大型/高风险、高难度/复杂任务，或用户明确要求审查时，按 `project-quality-gate` 执行 Final Audit。普通修改默认不创建 Final Audit 子任务。\n\nworkflow 侧契约：\n\n- `D:\\vibecoding\\subagent.md` 只作为 subagent 系统提示词，不是审查报告。\n- 审查记录必须落在 `D:\\vibecoding\\task` / `D:\\vibecoding\\log`，由 `workflow-task.ps1` 管理。\n- 主 agent 在创建审查子任务前，应把修改摘要、Verification 证据、跳过项和风险写入父 task。\n- 需要 Final Audit 时，只有 `audit-check: allow` 后才能父任务 complete。\n\n## Complete 要求\n\n父任务 `complete` 前必须满足：\n\n- 修改摘要清楚。\n- Acceptance Criteria 有对应 `item-done` 证据。\n- Flutter UI 普通修改及以上任务必须有 `代码语义完成度审查` 对应 `item-done` 证据，记录源码位置、Material/自定义控件取舍理由、用户需求映射、卡片/列表/页面层级/文案/动效结论；证据不足时不得 complete，即使构建、测试或截图通过。\n- 构建、运行测试、截图验证或服务端验证已按影响范围执行，或记录跳过原因。\n- 如需 Final Audit，已记录审查子 task id、审查 log 和 `audit-check: allow`。\n- 剩余风险和未验证项已写入 `-Risk` 或归档说明。\n\n完成示例：\n\n```powershell\npowershell.exe -NoProfile -ExecutionPolicy Bypass -File 'D:\\vibecoding\\command\\workflow-task.ps1' -Action complete -TaskId task-... -Validation \"...\" -Risk \"...\"\n```\n\n## 汇报要求\n\n最终回复只汇报对用户有用的 workflow 信息：task 是否开始和完成、log 路径、关键脚本结果、Final Audit 子任务和 log（若有）、剩余风险。\n";
