// Mechanical UTF-8 snapshot of flutter-app-design/SKILL.md.
// SHA256: 3E294D93C31230A1A6CA84F9190DD0DD52A10FDE978C4D2204630E7DD704A710
export const FLUTTER_DESIGN_SOURCE_SHA256 = "3E294D93C31230A1A6CA84F9190DD0DD52A10FDE978C4D2204630E7DD704A710";
export const FLUTTER_DESIGN_SOURCE_CONSTRAINT_COUNT = 57;
export const FLUTTER_DESIGN_SOURCE = "---\nname: flutter-app-design\ndescription: \"Use when Flutter app UI, Material Design, Material 3, Android/Windows Flutter targets, adaptive/responsive layout, interaction feedback, visible state-change motion, shared motion tokens/helpers/wrappers, navigation semantics, icons/assets, settings/list pages, code semantic completion review, app shell polish, platform ergonomics, Flutter shared resources, or Flutter-adjacent native/Rust/FFI changes are involved.\"\n---\n\n# Flutter App Design\n\nUse this skill as the Flutter app UI and platform-adaptation authority. The default implementation is one shared Flutter Material 3 widget tree. Shared Flutter UI does not require cross-target consistency proof by default; verify platform adaptation only when the change can affect it.\n\nCore rule: Flutter Material 3 and the current project implementation define layout meaning, navigation meaning, information hierarchy, icon semantics, state semantics, and motion intent. Android and Windows differ only where target ergonomics require it.\n\n## Triggers And Scope\n\n- Read local project rules first: `main/` indexes, then relevant `request/` rules.\n- Use this skill for Flutter UI, Material 3, Android/Windows Flutter targets, new pages, UI polish, adaptive layout, interaction states, icons/assets, animation/motion, app shell, settings/list pages, and platform-specific Flutter ergonomics.\n- Scope markers: `(wa)` means Android and Windows; `(w)` means Windows only; `(a)` means Android only. Bare suffixes `wa`, `w`, or `a` at the end of short task text mean the same when unambiguous.\n- If no marker is present, Flutter UI/page/navigation/resource work defaults to shared Flutter implementation, not duplicated platform UI.\n- Explicit Android-only or Windows-only instructions limit implementation and verification to that platform unless shared resources, responsive breakpoints, navigation/state, models/API, packaging, or native/Rust/FFI boundaries are touched.\n- Use `ui-ux-pro-max` only for product-level UX direction such as page families, dashboards, onboarding, settings systems, data-heavy screens, major redesigns, or complex flows.\n\n## Material Component Selection\n\n- Choose the Material 3 or existing project primitive before creating a custom control.\n- Navigation uses Material/project navigation components: compact layouts use bottom navigation or equivalent project shell; wide layouts use rail/sidebar/pane shell. Child switching uses tabs, segmented controls, lists, or detail panes, not fake top-level destinations.\n- Commands use Material button families by emphasis: filled, tonal, outlined, text, icon, and FAB. Each route may expose at most one high-emphasis create/add action, and only when a real primary task exists.\n- Inputs use `TextField` or project input primitives with label, hint, error, disabled, focus, keyboard, validation, and accessibility behavior.\n- Binary settings use `Switch` or `Checkbox`; numeric settings use slider/stepper/input; option sets use menus, chips, tabs, or segmented controls.\n- Dialogs, sheets, and menus must have clear purpose, cancel/escape path, action hierarchy, focus order, scrim/tonal surface, and loading/error behavior.\n- Lists, settings, profile, directory, and ordinary content rows are not cards by default. Use list flow, dividers, selected state, hover/focus/pressed state layers, and grouped surface bands.\n- Surfaces must map to project/Material token roles for page, grouped content, active item, modal, selected item, and raised content. Do not fake hierarchy with random opacity, blur, one-off color panels, or decorative borders.\n- Icons must come from Flutter `Icon`/`IconData`, project-owned shared assets, icon font, generated icon components, or the current asset pipeline. Do not use text characters, punctuation, words, or emoji as icons.\n\n## Flutter UI Restrictions\n\n- Preserve behavior, navigation meaning, state ownership, data flow, storage, route, API, Rust bridge, and native capability contracts unless the user explicitly asks for behavior change.\n- Keep one shared widget tree wherever practical. Use adaptive/responsive composition for Android/Windows differences instead of copying pages into platform-only implementations.\n- Organize new/refactored pages by page directory: `lib/pages/<page>/` for page entry/private widgets, `lib/shared/` or `lib/widgets/` for cross-page UI, and services/models outside page files.\n- Use stable breakpoints or project size-class helpers; do not use viewport-scaled font hacks or negative letter spacing.\n- Custom controls are allowed only for a real interaction, layout, animation, or platform adaptation need. They must preserve semantics, focus order, keyboard behavior, touch target, state layer/equivalent feedback, reduced-motion behavior, and disabled/loading/error states.\n- Visible text must name a real object, action, state, error, navigation target, grouping, or user decision. Do not add tutorial copy, marketing copy, empty introductions, placeholder descriptions, duplicate titles, fake content, fake metrics, or fake actions.\n- Cards/surfaces are semantic containers only. Prefer direct content flow, group headers, spacing, inset dividers, tonal surfaces, and state layers for ordinary settings, forms, lists, history, search, empty states, and page content.\n- Two-column layouts should not card both columns by default. If the left column is navigation, directory, filters, or a list, keep it as direct content flow; card the right column only for details, preview, properties, or an independent functional panel.\n- Same-kind two-column lists should use columns, dividers, selection states, and spacing instead of paired cards.\n- Do not nest cards inside cards unless the inner card is an independent repeated item and a simpler list/divider solution is worse.\n- Design references or generated images guide hierarchy, spacing, surfaces, state expression, color balance, density, and motion impression only. Implementation must use real app data paths, real routing, real state, real accessibility, and real interactions.\n\n## Data, Cache, And Local Persistence\n\n- User-owned app data must live under the app data root in `data/<category>/`, grouped by durable category. Examples include `data/settings/`, `data/favorites/`, `data/history/`, `data/search/`, `data/conversations/`, `data/communities/`, and `data/provider/`.\n- Windows data roots must resolve under the user AppData location for the app, and Android data roots must resolve under the app's internal support storage. Do not hard-code external absolute paths for runtime data.\n- Cache, temporary WebView data, generated thumbnails, transient downloads, build intermediates, and recomputable files are not user data and must not be written under `data/`.\n- Cache must use a dedicated cache folder, preferably the platform/app cache root grouped as `cache/<category>/` or an equivalent project cache root outside `data/`. Examples include `cache/webview/`, `cache/thumbnails/`, `cache/downloads/`, `cache/previews/`, and `cache/tmp/`.\n- New or changed persistence/cache code must go through a shared app data/cache path helper when the project has one. Do not add page-local path joins, duplicated platform root detection, parallel default-root functions, or scattered cache folders.\n- Settings pages and background sync features must save their own configuration under `data/settings/` and sync durable data by category. A backend link must not cause cache directories, temporary files, secrets in logs, or unrelated project files to be uploaded.\n- Provider/API configuration is local app data and belongs under a data category such as `data/provider/`. Do not store provider credentials in source code, task logs, screenshots, public indexes, or non-app-data fallback locations.\n- Verification for storage/cache changes must prove the concrete file paths for affected categories, prove that cache paths use the dedicated cache folder, and separately prove that cache paths are outside `data/`.\n\n## Motion And UIX Shared Entry\n\n- Any visible UI state change should attempt meaningful animation unless the user forbids animation, the surface is static text, or reduced-motion settings require a non-motion cue.\n- Cover motion for route enter/back, modal/sheet/dialog open and close, list insert/remove/reorder, loading-to-content, empty/error/retry/success, selection, submit, destructive confirmation, hover, focus, press, and progress where the state exists.\n- Before adding motion, search for project shared motion tokens, animation helpers, transition wrappers, shared animated components, or theme duration/easing definitions. Use them first.\n- If the same motion need appears in multiple places and no shared entry exists, add or propose a shared project entry instead of repeating page-local animation code.\n- Motion must explain cause and effect. Reject decorative loops, delayed staging, parallax, bouncing, or layout-shifting animation unless it clarifies hierarchy, feedback, or state change.\n- Reduced-motion behavior must remain explicit. Animation cannot be the only state carrier; provide color, icon, text, layout, or state-layer cues for the same meaning.\n- Non-trivial UI edits must define a short source-level design contract before implementation: route/task, component map, token/surface map, state matrix, motion entry, accessibility checks, and verification target. Keep it in workflow/task notes unless the project has a design-system artifact.\n\n## Platform And Native Boundaries\n\n- Android-sensitive changes include touch target size, safe areas/insets, system back, IME, rotation, permissions, lifecycle, portrait/landscape/tablet breakpoints, and packaging of mobile assets.\n- Windows-sensitive changes include hover, focus, keyboard traversal, shortcuts, high DPI, resizing, scrollbars, reduced motion, high contrast, desktop density, file dialogs, window constraints, and packaging of desktop assets.\n- Ordinary shared Flutter UI changes only need the smallest affected-platform check. Verify both Android and Windows only for responsive breakpoints, shared theme/fonts/icons/assets, navigation/state, model/API, platform adapters, native/Rust/FFI, packaging, or explicit cross-platform consistency work.\n- Do not add platform-specific product UI screens, WPF, WinUI, Qt, Avalonia, MAUI, UWP, Fluent-first, pure native Windows UI, C# backend paths, or parallel platform UI forks for ordinary Flutter product UI.\n- Do not introduce Rust, native code, or FFI for one-off UI glue or platform-exclusive presentation behavior.\n- Rust/native/FFI checks apply only when the task touches shared domain logic, bridge bindings, generated bindings, native artifacts, packaging, or platform adapters.\n- If a change touches duplicate entrypoints, compatibility wrappers, fallback branches, deprecated APIs/routes, duplicate adapters, or schema bridges, also use `remove-legacy-compat`.\n\n## Verification And Evidence\n\n- Run the smallest effective verification for the actual impact. Pure Q&A and read-only analysis are exempt.\n- For Material/UI quality work, source audit comes before screenshots: prove the widget tree uses the intended Material/project primitives, route hierarchy, token roles, state matrix, shared motion entry, and accessibility semantics.\n- Tiny text/color/spacing edits may be verified by source inspection when they do not affect layout, responsiveness, assets, navigation, state, motion, or platform behavior.\n- `(a)` Android-only impact: verify the affected Android profile such as portrait, landscape/tablet, insets, IME, back behavior, or mobile asset packaging.\n- `(w)` Windows-only impact: verify the affected Windows profile such as wide/narrow resizing, hover/focus, keyboard, scrollbars, high DPI, or desktop asset packaging.\n- `(wa)` or shared Flutter changes: verify shared source and the primary affected target; verify both targets only for responsive, asset/theme, navigation/state, model/API, native/Rust/FFI, packaging, or explicit cross-platform consistency work.\n- Screenshot comparison is required only when the user asks for visual comparison, a prior cross-target visual regression exists, or the change specifically targets visual alignment.\n- For Chinese UI copy, generated resource names, design notes, skill rules, and validation scripts, verify UTF-8 text is not mojibake before claiming completion.\n- Completion reports should record scope, affected Flutter/platform surfaces, source-inspection evidence, motion/shared-entry decision, verification commands/results or skipped checks, and remaining risk.\n";
